# 🧪 كيف تشغل الاختبارات - دليل سريع

## ✅ كل شيء جاهز ومثبت!

تم بالفعل إنشاء **135+ اختبار** لجميع الدوال السحابية في المشروع.

---

## 🚀 تشغيل الاختبارات (3 خطوات فقط)

### **الخطوة 1: افتح Terminal**
في VS Code: اضغط `` Ctrl + ` `` (أو `Terminal` → `New Terminal`)

### **الخطوة 2: انتقل لمجلد functions**
```bash
cd functions
```

### **الخطوة 3: شغّل الاختبارات**
```bash
npm test
```

**هذا كل شيء!** ✨

---

## 📋 أوامر إضافية مفيدة

```bash
# تشغيل الاختبارات مع قياس التغطية
npm run test:coverage

# تشغيل الاختبارات مع المراقبة المستمرة (يعيد التشغيل عند التعديل)
npm run test:watch
```

---

## 📁 أين تجد الملفات؟

في VS Code، ستجد:

```
functions/
├── test/                              👈 المجلد الرئيسي
│   ├── README.md                      📖 دليل شامل بالعربية
│   ├── setup.ts                       ⚙️ الإعدادات
│   ├── checkPermission.test.ts        ✅ 40+ اختبار
│   ├── getUserEffectivePermissions.test.ts ✅ 35+ اختبار
│   ├── manageUserPermissions.test.ts  ✅ 30+ اختبار
│   └── manageJobPermissions.test.ts   ✅ 30+ اختبار
├── .mocharc.json                      ⚙️ إعدادات Mocha
├── .nycrc.json                        ⚙️ إعدادات التغطية
└── tsconfig.test.json                 ⚙️ إعدادات TypeScript
```

---

## 📚 للمزيد من المعلومات

افتح الملف:
```
functions/test/README.md
```

يحتوي على:
- ✅ شرح مفصل لكل ملف اختبار
- ✅ كيفية كتابة اختبارات جديدة
- ✅ استراتيجية الاختبار
- ✅ حل المشاكل الشائعة

---

## ⚠️ ملاحظة مهمة

إذا ظهرت أخطاء عند تشغيل الاختبارات، قد تحتاج لتشغيل **Firestore Emulator**:

```bash
# في terminal منفصل
firebase emulators:start --only firestore
```

ثم في terminal آخر:
```bash
cd functions
npm test
```

---

## 🎉 النتيجة المتوقعة

عند تشغيل `npm test` بنجاح، سترى:
- ✅ جميع الاختبارات تعمل
- ✅ تفاصيل كل اختبار
- ✅ عدد الاختبارات التي نجحت

---

**🚀 ابدأ الآن:** `cd functions && npm test`
